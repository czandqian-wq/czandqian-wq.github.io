# 小数据精灵接入与维护说明书

> 适用项目：`04-项目实战/个人网站/`  
> 当前状态：已接入，默认保持本地开发，不在本次任务中重新部署。

## 1. 接入目标

小数据精灵是网站的轻量互动陪伴组件，用于强化“AI 数据训练与持续学习”的视觉记忆点：

- 页面静止时播放待机动画；
- 普通滚动时点头回应；
- 快速或连续滚动时进入敲键盘状态；
- 桌面端根据鼠标位置轻微倾斜；
- 浅色、深色主题均不出现矩形背景；
- 移动端缩小显示，不遮挡主要阅读区域；
- 开启“减少动态效果”后仅展示静态画面。

## 2. 最终素材

网站实际使用以下透明素材：

| 文件 | 用途 | 规格 |
|---|---|---|
| `public/bot/bot-idle-animated.webp` | 待机循环 | 透明动画 WebP，45 帧 |
| `public/bot/bot-nod-animated.webp` | 点头回应 | 透明动画 WebP，62 帧 |
| `public/bot/bot-work-animated.webp` | 敲键盘工作 | 透明动画 WebP，62 帧 |
| `public/bot/bot-poster.webp` | 静态降级画面 | 透明 WebP |
| `public/bot/bot-motion.json` | 动画尺寸、帧数与时长记录 | JSON |

三段动画统一为 `256 × 411`，避免动作切换时比例跳变。旧的图集和 MP4 仍作为原始成品留在 `public/bot/`，当前组件不再调用它们。

## 3. 视频背景色问题

### 根因

`bot-nod.mp4` 和 `bot-work.mp4` 使用 H.264 编码，不带 Alpha 透明通道。视频背景也不是单一的 `#FBE8BF`，而是带有亮度变化和渐变的米黄色区域。

因此下列做法不能解决根因：

- 给容器设置 `background: #FBE8BF`；
- 把 `object-fit` 改成 `cover`；
- 只加圆角或隐藏溢出；
- 在浅色和深色主题分别猜测一个接近的底色。

这些方法最多只能弱化边缘，深色主题下仍会看到完整矩形。

### 当前处理

素材包 `D:/AI-KnowledgeBase/下载/motion-bot/frames/` 已保留三段动画的透明 PNG 帧。项目通过 `scripts/build_data_buddy_assets.py` 将这些透明帧统一裁切、缩放并编码为透明动画 WebP。

重新生成命令：

```powershell
python scripts/build_data_buddy_assets.py "D:/AI-KnowledgeBase/下载/motion-bot"
```

因为最终展示的是透明像素，组件不依赖页面背景色，能够直接适配全部主题。

## 4. 组件结构

组件文件：`app/components/DataBuddy.tsx`

当前实现不再使用 Canvas 图集与不透明视频叠加，而是使用三段同规格的透明动画 WebP：

```tsx
import DataBuddy from "./components/DataBuddy";

<DataBuddy width={164} />
```

可用参数：

| 参数 | 默认值 | 说明 |
|---|---:|---|
| `width` | `164` | 桌面端目标宽度，移动端由 CSS 限制到 96px |
| `followMouse` | `true` | 仅精细指针设备启用鼠标倾斜 |
| `scrollAction` | `true` | 是否根据页面滚动切换动作 |
| `className` | 空字符串 | 附加样式类 |

## 5. 交互逻辑

### 出现范围

- 首屏滚动超过约 55% 后出现，避免与 Hero 右侧状态面板争抢视觉焦点；
- 联系区进入视口前自动离场，避免遮挡联系方式和页脚；
- 精灵未显示时不渲染动画图片。

### 滚动判断

组件使用 `requestAnimationFrame` 汇总滚动距离和速度，不直接依赖单个滚轮事件：

- 普通滚动：切换为点头状态，并保留到关键动作播放完成；
- 快速或连续滚动：切换为工作状态，并完整展示敲键盘动作；
- 同一种动作持续触发时只延长停留时间，不重复重载动画。

这种判断能兼容鼠标滚轮、触控板、平滑滚动和移动端触摸滚动。

### 主动互动

- 单击机器人：完整播放一次点头，并显示一条数据学习短句；
- 双击机器人：进入敲键盘工作状态；
- 长按机器人约 0.56 秒：进入工作状态，适用于移动端；
- 点击桌面端状态标签：打开或收起互动说明气泡；
- 键盘聚焦机器人后按 Enter 或空格：等同于单击；
- 气泡自动收起，不阻挡页面主体内容。

### 鼠标跟随

- 仅在 `(hover: hover) and (pointer: fine)` 的设备上启用；
- 指针事件通过 `requestAnimationFrame` 节流；
- 最大倾斜幅度受到限制，离开页面后自动回正；
- 移动端不会注册鼠标跟随监听器。

## 6. 主题与移动端

相关样式位于 `app/globals.css` 的 `.data-buddy-*` 区域。

浅色主题使用柔和的绿色投影和透明光圈。深色主题不改变素材背景，而是对精灵本体进行适度提亮，并添加青绿色边缘光和地面光晕，避免黑色机身融入深色页面。

移动端规则：

- 最大宽度为 96px；
- 隐藏状态文字，只保留精灵；
- 放在返回顶部按钮上方；
- 支持单击和长按，但互动区域仅限机器人本体，不阻挡页面其他按钮和滑动；
- 联系区出现前自动隐藏。

## 7. 性能与无障碍

- 首次只加载约 1.6MB 的待机动画；
- 点头和工作动画在精灵出现 500ms 后延迟预加载；
- 首屏和联系区之后不挂载动画图片；
- 素材加载失败时自动回退到静态海报；
- `prefers-reduced-motion: reduce` 下停用滚动动作和鼠标跟随；
- 状态容器有中文无障碍标签，视觉图片使用空 `alt`，避免重复朗读。

## 8. 验收清单

- [ ] 页面可正常构建，无 TypeScript 或导入错误；
- [ ] 首屏不显示精灵，向下滚动后自然出现；
- [ ] 普通滚动能够触发点头；
- [ ] 快速滚动能够触发敲键盘；
- [ ] 停止滚动后能够回到待机；
- [ ] 三种动作切换时尺寸基本一致；
- [ ] 浅色主题没有米黄色或白色矩形；
- [ ] 深色主题没有矩形背景，黑色机身仍清晰可见；
- [ ] 375px 宽度下不产生横向滚动，不遮挡主要按钮；
- [ ] 联系区出现前精灵自动隐藏；
- [ ] 开启“减少动态效果”后只显示静态海报；
- [ ] 素材加载失败时仍能看到静态降级画面。

## 9. 禁用做法

- 不再把不透明 MP4 直接覆盖到透明待机动画上；
- 不使用相近背景色冒充透明；
- 不在每个滚动事件中直接修改 React 状态或元素变换；
- 不在移动端启用鼠标跟随；
- 不让精灵覆盖联系区、弹窗或返回顶部按钮。
