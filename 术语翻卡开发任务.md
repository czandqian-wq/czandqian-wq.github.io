# 🃏 术语翻卡开发任务（给 Codex 的提示词）

> 请按本文件完成个人网站 PLAYGROUND 板块的改版：把"模拟标注实验"替换为"AI 术语翻卡"。
> 项目根目录：`04-项目实战/个人网站/`（Next.js + TypeScript + CSS，无 UI 框架）。

---

## 一、任务目标

把 `app/page.tsx` 中 `id="lab"` 的 section（当前是"PLAYGROUND / 模拟标注实验"）整体替换为**"AI 术语翻卡"**交互区：

- 访客看到一张卡片，正面是 AI 术语名（如 "Token"）
- 点击卡片 → 3D 翻转 → 背面展示该术语的三栏解释：**① 专业术语解释 ② 面试可简洁回答 ③ 生活化例子**
- 底部有「上一个 / 下一个」按钮 + 计数器（如 `03 / 12`），可浏览多张卡
- 卡片带 3D 翻转动画，风格与网站现有设计语言一致

**不要**保留任何"模拟标注/工作台"形式的内容（现有 PLAYGROUND 的示例文本、标签按钮、判断结果全部移除）。

---

## 二、数据（已备好，直接用）

数据文件：`app/glossary-data.ts`（已创建，包含 12 个精选术语，类型 `TermCard`）：

```ts
export type TermCard = {
  term: string;       // 术语名
  pro: string;        // ① 专业术语解释
  interview: string;  // ② 面试可简洁回答
  example: string;    // ③ 生活化例子
};
export const glossaryCards: TermCard[] = [ ... 12 条 ... ];
```

**直接 import 使用，不要改动数据内容。**

如果想扩充到 63 词，可从知识库读取：
`D:/AI-KnowledgeBase/AI数据训练师/02-知识Wiki/概念与主题/AI名词解释大全.md`
（表格格式：`| 名词 | ① 专业术语解释 | ② 面试可简洁回答 | ③ 生活化例子 |`，前 12 个已精选进 `glossary-data.ts`，扩充时保持同样结构追加）

---

## 三、实现要求

### 1. 结构（替换 `page.tsx` 中 id="lab" 的 section）

保持现有 section 外壳（`section-head` 编号 `05`），把标题从"PLAYGROUND / 模拟标注实验"改为：
- section-head 小标题：`PLAYGROUND / AI 术语翻卡`
- 大标题（左侧 lab-intro 区）：`一个术语，两种说法。`（或类似的简洁标题）
- 说明文字：`点击卡片翻面，看看每个 AI 术语的专业解释、面试回答和生活化例子。来自我的知识库：AI 名词解释大全（63 词）。`

右侧保留现有 `lab-panel` 容器作为翻卡舞台。

### 2. 翻卡交互（新增组件或内联实现均可）

建议在 `app/components/` 新建 `GlossaryFlipCard.tsx`（或直接在 page.tsx 内实现），状态：

```ts
const [index, setIndex] = useState(0);        // 当前卡片索引
const [flipped, setFlipped] = useState(false); // 是否已翻面
```

卡片结构（3D 翻转）：

```tsx
<div className="flip-card" onClick={() => setFlipped(!flipped)}>
  <div className={`flip-inner${flipped ? " flipped" : ""}`}>
    <div className="flip-face flip-front">
      {/* 正面：术语名，大字 */}
      <span>TERM / 术语</span>
      <h3>{card.term}</h3>
      <small>点击翻面查看解释</small>
    </div>
    <div className="flip-face flip-back">
      {/* 背面：三栏解释 */}
      <strong>① 专业解释</strong><p>{card.pro}</p>
      <strong>② 面试回答</strong><p>{card.interview}</p>
      <strong>③ 生活例子</strong><p>{card.example}</p>
    </div>
  </div>
</div>
```

导航（卡片下方）：

```tsx
<div className="flip-nav">
  <button onClick={() => { setFlipped(false); setIndex(i => Math.max(0, i - 1)); }} disabled={index === 0}>← 上一个</button>
  <span>{String(index + 1).padStart(2, "0")} / {String(glossaryCards.length).padStart(2, "0")}</span>
  <button onClick={() => { setFlipped(false); setIndex(i => Math.min(glossaryCards.length - 1, i + 1)); }} disabled={index === glossaryCards.length - 1}>下一个 →</button>
</div>
```

切卡时先重置 `flipped = false`（回到正面），并给卡片一个轻微的切卡动画（可选：key={index} 触发重挂载淡入）。

### 3. 样式（加到 `app/globals.css`）

沿用网站现有 design tokens（`:root` 里的 `--paper`/`--ink`/`--muted`/`--line`/`--accent`/`--accent-dark`/`--shadow`），风格与现有卡片一致（细边框 `1px solid var(--line)`、衬线字体、克制的阴影）。

3D 翻转核心样式：

```css
.flip-card { perspective: 1000px; }
.flip-inner {
  position: relative;
  transform-style: preserve-3d;
  transition: transform .6s cubic-bezier(.22,.8,.22,1);
}
.flip-inner.flipped { transform: rotateY(180deg); }
.flip-face {
  backface-visibility: hidden;
  -webkit-backface-visibility: hidden;
}
.flip-back { transform: rotateY(180deg); }
```

参考尺寸：卡片高度约 420-480px，宽度占满 `lab-panel` 内容区；背面文字排版参考现有 `lab-result` 的密度（`font-size 13px; line-height 1.6`），三栏之间用 `border-top: 1px solid var(--line)` 分隔。

**无障碍**：卡片加 `role="button"` + `tabIndex={0}` + `aria-label`（如 `Token，点击翻面`），支持 Enter 键翻面（`onKeyDown`）。

**响应式**：≤720px 时卡片高度可适当降低（约 380px），文字 `font-size` 降至 12px，确保 375px 宽不横向溢出。

### 4. 删除内容

移除 `page.tsx` 中 `annotationDemo` 相关的所有 JSX（示例文本区、label-buttons、lab-result）以及 `useState` 里不再使用的 `selectedLabel`。`annotationDemo` 数据如果只在 lab 用，可从 `content.ts` 移除（若其他位置引用则保留）。

---

## 四、验证清单（完成前逐项检查）

- [ ] `npm run build` 通过、`npx eslint app/` 无错误
- [ ] 浏览器打开：lab 区显示术语翻卡，不再是模拟标注实验
- [ ] 点击卡片 → 3D 翻转显示三栏解释
- [ ] 再次点击 → 翻回正面
- [ ] 「下一个/上一个」切换卡片，计数正确（01/12 起），边界按钮禁用
- [ ] 切卡后回到正面状态
- [ ] 键盘 Tab 到卡片 + Enter 可翻面
- [ ] 375px 宽度下卡片不横向溢出
- [ ] 深色模式下文字可读（注意 `--paper` 变量随主题变化）
- [ ] 卡片翻转动画流畅（60fps，无闪烁）

## 五、范围外（不要做）

- 不要改其他 section（Hero/About/Projects/Growth/Roadmap/Contact）
- 不要改 `glossary-data.ts` 的数据内容
- 不要引入新的 npm 依赖（纯 CSS 翻转即可）
- 不要动 DataBuddy / GrowingSapling 组件
