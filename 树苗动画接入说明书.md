# 数据树苗滚动动画：接入与维护说明

> 适用项目：`04-项目实战/个人网站/`
>
> 当前状态：已接入本地 Next.js 网站，位于“研究与经历”和“模拟标注实验”之间。

## 1. 设计目的

数据树苗不是单独的装饰视频，而是网站叙事的一部分：从土木工程研究走向 AI 数据训练，能力通过资料、数据、规则和复盘逐步生长。

访客向下滚动时，画面由种子成长为大树；向上滚动时可以反向查看。系统启用“减少动态效果”后，组件直接展示静态完成画面，不再跟随滚动。

## 2. 当前页面位置

网站内容顺序如下：

1. 关于七安
2. 研究与经历
3. 数据与成长（树苗动画）
4. 模拟标注实验
5. 研究与项目
6. 公开路线图

不建议把树苗放进 Hero。首屏已有深浅主题互动面板，而且首屏滚动距离较短，会造成内容拥挤和动画生长过快。

## 3. 文件与素材

| 文件 | 用途 |
|---|---|
| `app/components/GrowingSapling.tsx` | 滚动驱动、懒加载、移动端适配和无障碍逻辑 |
| `app/page.tsx` | “数据与成长”章节及组件调用位置 |
| `app/globals.css` | 页面融合、主题变量、加载与移动端样式 |
| `public/motion/motion-scrub-desktop.mp4` | 原始桌面素材，保留作来源 |
| `public/motion/motion-scrub-mobile.mp4` | 原始移动素材，保留作来源 |
| `public/motion/motion-scrub-desktop-light.mp4` | 校准后的浅色桌面素材 |
| `public/motion/motion-scrub-mobile-light.mp4` | 校准后的浅色移动素材 |
| `public/motion/motion-scrub-desktop-dark.mp4` | 深色桌面素材，背景与网站夜色匹配 |
| `public/motion/motion-scrub-mobile-dark.mp4` | 深色移动素材，背景与网站夜色匹配 |
| `public/motion/compile.json` | 生成信息，只读参考 |

两段成品视频均为 98 帧、24 fps、约 4.08 秒，画面比例为 **4:7**。不要改成 9:16，否则会产生轻微裁切或形变。

## 4. 当前调用方式

页面中使用：

```tsx
import GrowingSapling from "./components/GrowingSapling";

<GrowingSapling theme={theme} width={420} smoothTime={0.1} />
```

可用参数：

| 参数 | 默认值 | 说明 |
|---|---:|---|
| `theme` | 必填 | `light` 或 `dark`，用于选择对应视频 |
| `width` | `320` | 期望显示宽度，组件同时受 `max-width: 100%` 约束 |
| `smoothTime` | `0.1` | 滚动阻尼时间；越小越跟手，越大越柔和 |
| `frameCount` | `98` | 视频帧数，素材不变时不要修改 |
| `className` | 空 | 追加自定义类名 |

## 5. 已包含的稳定性处理

### 5.1 移动端素材选择

组件挂载后先通过 `(max-width: 720px)` 判断设备，再设置视频地址。初始阶段不直接请求桌面视频，避免手机重复下载桌面和移动两份素材。

### 5.2 临近视口才加载

树苗距离视口约 600px 时才开始加载。这样首屏不会立即承担额外视频流量，同时能在访客滚动到章节前提前准备素材。

### 5.3 离开视口后暂停计算

组件使用 `IntersectionObserver` 判断可见性。动画离开视口后会停止 `requestAnimationFrame`；重新进入视口时恢复，减少后台 CPU 和耗电。

### 5.4 减少动态效果

当系统启用 `prefers-reduced-motion: reduce` 时：

- 不注册滚动驱动动画；
- 不持续执行逐帧计算；
- 直接展示树苗成长完成后的静态画面。

### 5.5 加载与失败降级

- 视频加载前显示“数据树加载中…”；
- 视频加载失败时显示“数据树暂时未能加载”；
- 桌面与移动素材切换时会重置加载状态，避免旧状态残留。

### 5.6 无障碍

- 容器使用语义化 `<figure>` 和完整中文说明；
- 视频本身设置 `aria-hidden="true"`；
- 加载与失败提示使用 `role="status"`；
- 动画不接管滚轮、触摸或键盘，不阻塞页面浏览。

## 6. 主题兼容策略

视频内部是不透明背景，不能只通过 CSS 改变页面颜色。项目保留原始素材，并额外生成两份色彩校准后的浅色素材和两份深色专用素材：只调整接近背景色的像素，保留青绿色树冠、金色树干和深色土地。

组件接收页面的 `theme`，根据主题和视口共同选择四份成品素材中的一份。章节本身使用与网站一致的主题变量，并去掉树苗外层的边框、底板、旋转和阴影。视频左右边缘还有一层同色羽化，用于吸收 H.264 色彩转换和压缩产生的细微色差。

## 7. 调整动画速度

优先调整 `smoothTime`：

```tsx
<GrowingSapling theme={theme} smoothTime={0.07} /> // 更跟手
<GrowingSapling theme={theme} smoothTime={0.14} /> // 更柔和、更拖尾
```

树苗不使用额外滚动轨道，也不使用 sticky 固定。组件根据自身在视口中的真实位置计算进度：树苗底部到达视口 92% 时开始生长，树苗顶部到达视口 6% 时完成生长。

```ts
const startTop = viewportHeight * 0.92 - rect.height;
const endTop = viewportHeight * 0.06;
const progress = clamp((startTop - rect.top) / (startTop - endTop));
```

这意味着视频完整进入屏幕前始终是种子，随后利用自然页面滚动完成生长；没有人为增加的页面高度和空白区域。CSS 把树苗高度限制在 `58svh` 以内，因此完成时树冠与根部仍处于屏幕可见范围。修改后必须同时检查桌面和手机。

## 8. 验证清单

每次修改树苗组件、视频素材或章节布局后，应完成以下检查：

- [ ] 网站可以正常构建，自动测试通过；
- [ ] 浅色主题加载浅色树苗，深色主题加载深色树苗；
- [ ] 向下滚动时树苗生长，向上滚动时反向缩回；
- [ ] 树苗离开视口后不继续进行高频逐帧计算；
- [ ] 375px 宽度下使用移动视频，页面没有横向溢出；
- [ ] 每种主题下，桌面端只请求桌面素材，移动端只请求移动素材；
- [ ] 开启“减少动态效果”后显示静态完成画面；
- [ ] 视频加载失败时仍有可理解的文字提示；
- [ ] 页面滚动、主题切换、项目弹窗等原有交互不受影响。

## 9. 常见问题

| 现象 | 可能原因 | 处理方式 |
|---|---|---|
| 视频没有出现 | 素材路径或文件名错误 | 检查 `public/motion/` 下的两个 MP4 |
| 一直显示加载中 | 视频未完成首帧解码或格式不支持 | 查看浏览器网络和媒体错误，确认 H.264 MP4 可播放 |
| 深色主题中仍是米白画布 | `theme` 未传入或深色素材缺失 | 检查组件参数和两个 `-dark.mp4` 文件 |
| 生长速度不合适 | 阻尼或映射区间不合适 | 先调 `smoothTime`，再调 `start/end` |
| 手机加载了两份视频 | 视频地址在设备判断前被设置 | 保持当前“先判断视口、再赋值”的实现 |
| 页面滚过树苗后仍耗电 | 离屏暂停逻辑被删除 | 恢复组件内的可见性观察和 RAF 取消逻辑 |
| 视频画面轻微变形 | 比例被写成 9:16 | 恢复 `aspect-ratio: 4 / 7` |

## 10. 维护原则

1. `GrowingSapling.tsx` 是组件逻辑的唯一事实来源，本说明不再复制整份源码，避免两份代码不一致。
2. 替换视频时，应同步制作浅色与深色、桌面与移动四份成品，并核对宽高比、帧数、fps、时长和文件体积。
3. 不要为了动画删除“减少动态效果”、失败提示或离屏暂停。
4. 不要将本地绝对素材路径写进页面代码；网站只引用 `/motion/...` 公共路径。
5. 当前素材包原始文件保存在 `D:\AI-KnowledgeBase\下载\motion-sapling\`，项目内只保留网站运行所需成品。
